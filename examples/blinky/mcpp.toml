[package]
name    = "blinky"
version = "0.1.0"

# So plain `mcpp run` already cross-compiles.
[build]
target = "riscv64-none-elf"

[dependencies]
mcpplibs.riscv-virt-rt = { path = "../.." }

[targets.firmware]
kind = "bin"
main = "src/main.cpp"
